# Demo output provenance

This folder is a **tiny n=100 hand-demo**, not the fitted samples.

For the authoritative per-table catalog (all 28 CLIF 2.1 tables), packs
(`base_pack` vs `icu_all28`), and the fitted ICU realism changelog, see the
repo-root [`PROVENANCE.md`](../PROVENANCE.md).

Committed realistic samples:

- [`sample_dataset/`](../sample_dataset/) — ICU, `icu_all28` + `recalibrate_fitted_icu`
- [`sample_full_hospital/`](../sample_full_hospital/) — whole-hospital mix
