# Demo output provenance

This folder is a **tiny n=100 hand-demo**, not the MIMIC-calibrated samples.

For the authoritative per-table catalog (all 28 CLIF 2.1 tables + latent spine),
packs (`base_pack` vs `mimic_all28`), and the MIMIC realism changelog, see the
repo-root [`PROVENANCE.md`](../PROVENANCE.md).

Committed realistic samples:

- [`sample_dataset/`](../sample_dataset/) — ICU, `mimic_all28` + `recalibrate_mimic_icu`
- [`sample_full_hospital/`](../sample_full_hospital/) — whole-hospital mix
